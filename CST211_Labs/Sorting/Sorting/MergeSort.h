#ifndef MERGESORT_H
#define MERGESORT_H

#include "ISort.h"

template<typename RT, typename T>
class MergeSort : ISort<RT>
{
	MergeSort(RT & ra, int length);

private:
	virtual void Sort(RT & ra, int length)
};

template<typename RT, typename T>
MergeSort<RT, T>::MergeSort(RT & ra, int length)
{

}

template<typename RT, typename T>
class MergeSort : ISort < RT >
{
	MergeSort(RT & ra, int length);

	:
	void Sort(RT & ra, int length) MergeSort::Sort(RT & ra, int length) private: void Sort(RT & ra, int length)
}
{

}

#endif // MERGESORT_H
